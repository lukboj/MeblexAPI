@model HomeViewModel


<h2>Polecane Meble</h2>
<h4>Wysoka jakość i przystępna cena</h4>

<div class="row">
    @foreach (var mebel in Model.PrefferedProducts)
    {
        @Html.Partial("ProductSummary", mebel)
    }       

</div>